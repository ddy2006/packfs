# pull-untar

## 模块功能

- 数据拷贝到处理节点
- 按秒数据解包为dat文件
- 考虑流控，设置最大读取带宽
- 如果有解包操作，删除中间文件

## 任务体body

body是与source_root相对的文件相对路径

## 主要参数表

参数名       | 参数说明
----------  | --------------------
source_root | 源路径根目录
target_root | 目标路径根目录
bw_limit    | 读取带宽的最大值(MB/s，kB/s)，比如500k,10m

- 参数值可以从任务头、环境变量中读取
